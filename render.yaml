services:
  - type: web
    name: nezha-dashboard
    env: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /
    autoDeploy: true
    plan: starter
    envVars:
      - key: NZ_LISTEN_HOST
        value: 0.0.0.0
      - key: NZ_LOCATION
        value: Asia/Shanghai
      - key: NZ_TSDB_DATA_PATH
        value: /dashboard/data/tsdb
      - key: TZ
        value: Asia/Shanghai
    disk:
      name: nezha-data
      mountPath: /dashboard/data
      sizeGB: 1
